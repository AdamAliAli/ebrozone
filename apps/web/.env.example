# Base URL of the NestJS API (apps/api). Server-only — read only inside
# Route Handlers/Server Actions/Server Components. Never prefix this with
# NEXT_PUBLIC_, it must never be exposed to browser JavaScript.
API_URL="http://localhost:4000"
